name: Keep Render App Awake

on:
  schedule:
    # Runs every 14 minutes to prevent Render's 15-minute inactivity sleep
    - cron: '*/14 * * * *'
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Health Endpoint
        run: |
          # Automatically ping the Render health endpoint to prevent free tier sleep
          curl -sSf https://persona-ai-0jrl.onrender.com/api/health > /dev/null
          echo "Ping successful!"
