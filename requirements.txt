# requirements.txt - Minimal for Railway (must be under 4GB)

# Web framework (lightweight)
Flask==3.0.0
Flask-CORS==4.0.0
gunicorn==21.2.0

# PDF processing
PyMuPDF==1.23.26

# Use sentence-transformers without explicit torch (let it install minimal deps)
# This avoids the 6GB torch installation
sentence-transformers==2.2.2

# Numerical operations
numpy==1.24.4
scikit-learn==1.3.2